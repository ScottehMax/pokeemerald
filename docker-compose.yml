services:
  builder:
    build: .
    image: pokeemerald-builder
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      RUST_TARGET: thumbv4t-none-eabi
    command: make
